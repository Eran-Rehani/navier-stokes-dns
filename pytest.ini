[pytest]
testpaths = tests
# The cpp marker builds + runs the CPU DNS (needs cmake + FFTW3); it is
# skipped by default.  Run it with:  pytest -m cpp
addopts = -m "not cpp"
markers =
    cpp: builds and runs the ns3d_cpu binary for cross-Backend checks
